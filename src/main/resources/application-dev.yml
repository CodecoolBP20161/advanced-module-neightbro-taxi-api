spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: jelszo
        url: jdbc:postgresql://localhost:5432/neighbro
        username: Kalman
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQL9Dialect
